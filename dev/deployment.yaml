apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 2
  template:
    metadata:
      labels:
        app: myapp
    spec:
      imagePullSecrets:
      - name: ecr-registry-key
      containers:
      - name: myapp
        image: nginx
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
