apiVersion: apps/v1
kind: Deployment
metadata:
  name: myapp
spec:
  selector:
    matchLabels:
      app: myapp
  replicas: 1
  template:
    metadata:
      labels:
        app: myapp
    spec:
      imagePullSecrets:
      - name: ecr-registry-key
      containers:
      - name: myapp
        image: busybox
        imagePullPolicy: Always
        ports:
        - containerPort: 8080
